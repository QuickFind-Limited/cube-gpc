cubes:
  - name: dim_payment_method
    sql: SELECT * FROM `gpc_data_2026_01_26.dim_payment_method`
    title: Payment Methods
    description: >
      Payment method dimension including credit cards, cash, and electronic payment types.
      Grain: one row per payment method.

    measures:
      - name: payment_method_count
        type: count
        description: Total number of payment methods

    dimensions:
      - name: payment_method_id
        sql: "{CUBE}.payment_method_id"
        type: number
        primary_key: true
        description: Unique identifier for the payment method

      - name: payment_method_name
        sql: "{CUBE}.payment_method_name"
        type: string
        description: Name of the payment method (e.g., Visa, Mastercard, Cash, PayPal)

      - name: is_inactive
        sql: "{CUBE}.is_inactive"
        type: boolean
        description: Indicates if the payment method is inactive

    segments:
      - name: active_methods
        sql: "{CUBE}.is_inactive = false"
        description: "Payment methods currently available for use"

      - name: inactive_methods
        sql: "{CUBE}.is_inactive = true"
        description: "Payment methods that have been deactivated"
