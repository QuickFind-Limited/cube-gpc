cubes:
  - name: departments
    sql_table: gpc_data_2026_01_26.departments
    title: Departments
    description: >
      Department reference data (eCommerce, Retail, Wholesale, etc.).
      Grain: one row per department.

    measures:
      - name: department_count
        type: count
        description: Total number of departments

      - name: active_department_count
        type: count
        filters:
          - sql: "{CUBE}.is_active = true"
        description: Number of active departments

    dimensions:
      - name: department_id
        sql: "{CUBE}.department_id"
        type: number
        primary_key: true
        description: Unique identifier for the department

      - name: department_name
        sql: "{CUBE}.department_name"
        type: string
        description: Department name (eCommerce, Retail, Wholesale)

      - name: is_active
        sql: "{CUBE}.is_active"
        type: boolean
        description: Whether department is active

    segments:
      - name: active
        sql: "{CUBE}.is_active = true"
        description: "Active departments only"

    pre_aggregations:
      - name: department_analysis
        type: rollup
        measures:
          - department_count
          - active_department_count
        dimensions:
          - department_name
        refresh_key:
          every: 1 day
