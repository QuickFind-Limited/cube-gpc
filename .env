# Cube.js Configuration
# Set to true to use BigQuery instead of DuckDB
USE_BIGQUERY=true

# BigQuery Configuration (when USE_BIGQUERY=true)
BIGQUERY_PROJECT_ID=gym-plus-coffee
BIGQUERY_LOCATION=US

# GCS Credentials (for DuckDB mode with parquet files)
# GCS_KEY_ID=your-key-id
# GCS_SECRET=your-secret
